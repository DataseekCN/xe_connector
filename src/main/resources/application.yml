
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: {database username}
    password: {database password}
    url: jdbc:mysql://{database url}/{database name}?characterEncoding=utf-8&useSSL=false
jpa:
  show-sql: true

server:
  servlet:
    context-path: /api